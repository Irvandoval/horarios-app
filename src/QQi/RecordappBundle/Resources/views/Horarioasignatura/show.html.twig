{% extends 'QQiRecordappBundle::layout.html.twig' %}

{% block title %}Horarioasignatura {% endblock %}

{% block body %}

<div class="container">
<div class="row">
    <div class="span12">

        <table class="table table-bordered table-striped">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Correlativo</th>
            <td>{{ entity.correlativo }}</td>
        </tr>
        <tr>
            <th>Horario</th>
            <td>{{ entity.idHorario }}</td>
        </tr>
        <tr>
            <th>Materia</th>
            <td>{{ entity.idAsignatura }}</td>
        </tr>
    </tbody>
</table>

    </div>
</div>

<div class="row">
        <a class="btn btn-primary btn-sm" href="{{ path('yes') }}">
            Regresar a listado
        </a>

        <a class="btn btn-success btn-sm"  href="{{ path('yes_edit', { 'id': entity.id }) }}">
           Modificar
        </a>


        <form action="{{ path('yes_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <br />
            <input class="btn btn-danger btn-sm" type="submit" value="Eliminar">
        </form>

</div>
</div>

{% endblock %}
