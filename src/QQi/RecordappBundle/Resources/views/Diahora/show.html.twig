{% extends 'QQiRecordappBundle::layout.html.twig' %}

{% block title %}Diahora {% endblock %}

{% block body %}

<div class="container">
<div class="row">
    <div class="span12">

        <table class="table table-bordered table-striped">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Nombre</th>
            <td>{{ entity.nombre }}</td>
        </tr>
        <tr>
            <th>Dia</th>
            <td>{{ entity.dia }}</td>
        </tr>
        <tr>
            <th>Hora</th>
            <td>{{ entity.hora|date('G:ia') }}</td>
        </tr>
        <tr>
            <th>Ciclo</th>
            <td>{{ entity.idCiclo }}</td>
        </tr>
    </tbody>
</table>

    </div>
</div>

<div class="row">
        <a class="btn btn-primary btn-sm" href="{{ path('diahora') }}">
            Regresar a listado
        </a>

        <a class="btn btn-success btn-sm"  href="{{ path('diahora_edit', { 'id': entity.id }) }}">
           Modificar
        </a>


        <form action="{{ path('diahora_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <br />
            <input class="btn btn-danger btn-sm" type="submit" value="Eliminar">
        </form>

</div>
</div>

{% endblock %}
