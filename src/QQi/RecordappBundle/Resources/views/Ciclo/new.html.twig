<h1>Ciclo creation</h1>

<form action="{{ path('ciclo_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<div class="row">
        <a href="{{ path('ciclo') }}">
            <i class="icon-th-list"></i> Regresar a listado
        </a>
</div>
